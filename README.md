# Timor-Leste Fiscal BVAR

This repository contains the data-processing pipeline, Bayesian Vector
Autoregression (BVAR) estimates, impulse-response functions (IRFs), forecast
error variance decompositions (FEVDs), and visual outputs used to study fiscal
transmission and fiscal leakage in Timor-Leste.

The central question is how public spending affects non-oil GDP and
what portion of that spending is related to imports.

## Why quarterly data are constructed

Timor-Leste has a relatively short macroeconomic history and most of the
available national accounts and monetary indicators are reported annually,
not monthly or quarterly. To estimate a quarterly BVAR without treating
annual observations as if they were high-frequency data, the project
disaggregates the annual series using temporal-disaggregation methods.

The exception is the import indicator: quarterly import data were obtained
from [Trade Map](https://www.trademap.org) and used as a high-frequency
indicator. The Trade Map economy group combines Timor-Leste's imports from:

- Indonesia
- China
- Australia
- Singapore
- Hong Kong, China
- Macao, China
- Chinese Taipei (Taiwan)
- Malaysia
- United States of America
- India
- Viet Nam

The source also contains an Australia entry that was not found in the final
search; it is documented here for transparency and is not treated as an
additional series.

Quarterly imports are used as the indicator in the Chow-Lin disaggregation of
annual non-oil GDP and annual imports. Government expenditure is disaggregated
with Denton-Cholette using no external indicator. CPI and private credit are
also converted to quarterly frequency with Denton-Cholette. The annual
constraints are preserved using sum conversion for flow variables and average
conversion for index or stock variables.

The resulting sample runs from **2002 Q1 to 2023 Q4**.

## Variables and units

The BVAR includes five endogenous variables:

| Variable | Description |
| --- | --- |
| `ln_gov_exp` | Government final consumption expenditure |
| `ln_gdp_non` | Real non-oil GDP |
| `ln_imports` | Real imports of goods and services |
| `ln_cpi` | Consumer Price Index |
| `ln_credit` | Claims on the private sector (private credit) |

Variables are in natural logarithms. Monetary variables (except CPI) are
measured in millions of US dollars; CPI is an index.

The model also includes five exogenous shock-window indicators based on
historical events and the structural-break analysis:

`shock_2006`, `shock_2009`, `shock_2012`, `shock_2016`, and `shock_covid`.
These represent temporary windows around the 2006 political crisis, the 2009
global financial crisis and petroleum-fund withdrawals, the post-United
Nations period, the 2016-2018 political crisis, and the COVID-19 period.

## Data sources

- Annual macroeconomic indicators: [Asian Development Bank Key Indicators -
  Timor-Leste](https://kidb.adb.org/economies/timor-leste), stored as
  `dataset/tim-key-indicators-2025.xlsx`.
- Quarterly import indicator: [Trade Map - countries/economy
  groups](https://www.trademap.org), stored as
  `dataset/timor-leste-imports-exports-to-timor-leste-by-product_all.csv`.

The raw source files are retained in `dataset/`. Intermediate cleaned and
model-ready files are written to `csv/`.

## Repository structure

```text
dataset/ Raw source files and source notes
csv/ Cleaned, quarterly, IRF, and FEVD data
graphics/ PDF figures generated by the analysis
scripts/ Reproducible R and Python analysis scripts
dashboard/ Shiny dashboard and dashboard-ready IRF data
requirements.txt
```

## Analysis workflow

Run the scripts in the following order:

1. `scripts/01_cleaning.R` reads the ADB workbook, selects the five annual
   macroeconomic series, cleans missing values, and converts the Trade Map
   import file into a quarterly import indicator. The script also applies the
   documented correction to the unusually high 2010 Q4 import observation.
2. `scripts/02_dessagregation.R` creates quarterly levels and log series from
   the annual data and the quarterly import indicator.
3. `scripts/03_structural_failures.R` estimates Bai-Perron breakpoints under
   the BIC criterion and creates the temporary shock-window dummies.
4. `scripts/04_stationary_test.R` runs ADF, KPSS, and Zivot-Andrews tests.
5. `scripts/05_graphics.R` creates the macroeconomic level and log-scale
   figures.
6. `scripts/06_hyperparameters_lag_bvar_test.R` compares lag lengths from one
   to six using BVAR log-marginal likelihoods.
7. `scripts/07_irf_fevd.R` estimates the BVAR, generates IRFs and FEVDs, and
   exports the corresponding tables for two Cholesky orderings.
8. `scripts/08.5_irf_combined.py` combines selected government-shock figures
   into a single PDF.
9. `scripts/08_view_irf_data.R` formats IRF tables and generates the
   significance summary in `significant_irf_automatic.md`.

The complete sequence is also defined in
`.github/workflows/reproducibility.yml` and is used by the repository's
reproducibility workflow.

## BVAR specification

The main estimation uses two lags and a Minnesota prior. The sampler is run
with 300,000 draws, 100,000 burn-in draws, and thinning every 50 draws.
Impulse responses are calculated for 20 quarters with 68% and 80% credible
intervals.

Two recursive Cholesky orderings are estimated to assess sensitivity to the
identification assumption:

1. Government expenditure, imports, non-oil GDP, CPI, private credit.
2. Non-oil GDP, government expenditure, imports, CPI, private credit.

The files `significant_irf.md` and `significant_irf_automatic.md` provide
additional summaries of responses whose credible intervals exclude zero.

## Outputs

- `csv/timor_annual_macro.csv`: cleaned annual macroeconomic data.
- `csv/imports_cleaning_timor_leste.csv`: cleaned quarterly import indicator.
- `csv/timor_quarterly_data.csv`: quarterly variables in levels.
- `csv/timor_quarterly_log.csv`: quarterly variables in natural logarithms.
- `csv/timor_quaiterly_log_and_dummys.csv`: log data plus exogenous dummies.
- `csv/irf_*_order_*.csv`: standard and cumulative IRF quantiles.
- `csv/fevd_order_*.csv`: FEVD quantiles.
- `graphics/`: macroeconomic figures, IRF figures, and FEVD figures.

## Interactive dashboard

The published Shiny dashboard allows users to choose the impulse, response,
IRF type, and Cholesky ordering:

[Open the Timor-Leste BVAR IRF dashboard](https://connect.posit.cloud/em3r/content/01a068c1-99da-638d-1ba0-1254293e5a61)

The dashboard source is `dashboard/app.R`. To run it locally, open R from the
`dashboard/` directory and run:

```r
shiny::runApp()
```

## Reproducing the project

The analysis requires R 4.4 or later, the R packages used in the scripts
(including `tidyverse`, `tempdisagg`, `BVAR`, `bvartools`, `strucchange`,
`tseries`, `urca`, and `shiny`), and Python 3 with Pillow. Python
dependencies are listed in `requirements.txt`; Poppler is required by the
PDF-to-PNG step in `08.5_irf_combined.py`.

From the repository root, the workflow can be run with:

```bash
Rscript scripts/01_cleaning.R
Rscript scripts/02_dessagregation.R
Rscript scripts/03_structural_failures.R
Rscript scripts/04_stationary_test.R
Rscript scripts/05_graphics.R
Rscript scripts/06_hyperparameters_lag_bvar_test.R
Rscript scripts/07_irf_fevd.R
python scripts/08.5_irf_combined.py
Rscript scripts/08_view_irf_data.R
```

The estimates depend on the source data, the temporal-disaggregation
assumptions, the selected prior, and the recursive ordering. Results should
therefore be interpreted as model-based evidence for a data-limited economy,
not as a substitute for official quarterly national accounts.
